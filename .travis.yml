language: java
sudo: required
dist: xenial
jdk:
  - openjdk11

env:
  global:
    - TOKEN=TOKEN

cache:
  directories:
    - $HOME/.m2/repository

script:
  - ./mvnw test -e

after_success:
  - ./mvnw clean test jacoco:report coveralls:report